name: 📦 Build Openwrt_x86-64 24.10.X

on:
  workflow_dispatch:
    inputs:
      luci_version:
        description: "选择luci版本"
        required: false
        default: "24.10.5"
        type: choice
        options:
          - 24.10.0
          - 24.10.1
          - 24.10.2
          - 24.10.3
          - 24.10.4
          - 24.10.5          
      custom_router_ip:
        description: "请设置路由器的管理地址(仅对多网口路由器有效) 格式:192.168.x.1 或 10.x.x.1"
        required: true
        default: "192.168.5.1"
      profile:
        description: '请输入要编译固件大小 单位(MB)'
        required: true
        default: '2048'
      enable_store:
        description: "集成 store 商店"
        required: false
        type: boolean
        default: false
      include_docker:
        description: |
          是否编译 Docker 插件
        required: true
        default: 'no'
        type: choice
        options:
          - 'yes'
          - 'no'
      enable_pppoe:
        description: "是否配置PPPoE拨号信息?"
        required: true
        default: 'no'
        type: choice
        options:
        - 'yes'
        - 'no'
      pppoe_account:
        description: "宽带账号 (若启用PPPoE)"
        required: false
      pppoe_password:
        description: "宽带密码 (若启用PPPoE)"
        required: false
  
jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set executable permissions
        run: chmod +x ${{ github.workspace }}/x86-64/build_config.sh

      - name: Install related software
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev zstd curl unzip tree

      - name: Save Custom Router IP into OpenWrt files
        run: |
          CONFIG_PATH="./files/uci-defaults/99-custom.sh"
          echo "当前网络配置路径: $CONFIG_PATH"
        
          # 1. 替换版本号占位符
          sed -i "s/版本号/${{ inputs.luci_version }}/g" "$CONFIG_PATH"
          [ -f "./files/etc/banner" ] && sed -i "s/版本号/${{ inputs.luci_version }}/g" "./files/etc/banner"

          # 2. 网络逻辑配置 (关键修改点：不再删除行，改用关键词替换)
          sed -i "s/192.168.5.1/${{ inputs.custom_router_ip }}/g" "$CONFIG_PATH"

          # 3. 替换脚本末尾的描述信息
          sed -i "s/VERXXXX/${{ inputs.luci_version }}/g" "$CONFIG_PATH"
        
          echo "✅ 配置修改完成，输出预览："
          cat "$CONFIG_PATH"
      
      - name: Validate PPPoE Inputs
        run: |
          if [[ "${{ inputs.enable_pppoe }}" == "yes" ]]; then
            if [[ -z "${{ inputs.pppoe_account }}" || -z "${{ inputs.pppoe_password }}" ]]; then
              echo "Error: PPPoE account and password must be provided when PPPoE is enabled!"
              exit 1
            fi
          fi
      
      - name: Enable Store integration
        run: |
          if [ "${{ github.event.inputs.enable_store }}" == "true" ]; then
            echo 'CUSTOM_PACKAGES="$CUSTOM_PACKAGES luci-app-store"' >> shell/custom-packages.sh
            echo "✅ 已追加 luci-app-store"
          else
            echo "❎ 未启用 luci-app-store"
          fi

      - name: Build openwrt-x86-64-efi
        run: |
          profile="${{ github.event.inputs.profile }}"
          include_docker="${{ github.event.inputs.include_docker }}"
          openwrt_version="${{ github.event.inputs.luci_version }}"
          echo "Building for profile: $profile"
          docker run --rm -i \
            --user root \
            -v "${{ github.workspace }}/bin:/home/build/openwrt/bin" \
            -v "${{ github.workspace }}/files:/home/build/openwrt/files" \
            -v "${{ github.workspace }}/custom:/home/build/openwrt/files/etc/config" \
            -v "${{ github.workspace }}/x86-64/openwrt.config:/home/build/openwrt/.config" \
            -v "${{ github.workspace }}/shell:/home/build/openwrt/shell" \
            -v "${{ github.workspace }}/x86-64/build_config.sh:/home/build/openwrt/build.sh" \
            -e PROFILE=$profile \
            -e INCLUDE_DOCKER=$include_docker \
            -e ENABLE_PPPOE=${{ inputs.enable_pppoe }} \
            -e PPPOE_ACCOUNT=${{ inputs.pppoe_account }} \
            -e PPPOE_PASSWORD=${{ inputs.pppoe_password }} \
          openwrt/imagebuilder:x86-64-openwrt-${openwrt_version} /bin/bash /home/build/openwrt/build.sh         

      - name: Create info
        run: |
          cat > ${{ github.workspace }}/router-info.md <<EOF
          ## 📡 路由器后台信息
          
          - 🏠 **后台地址**: ${{ github.event.inputs.custom_router_ip }}
          - 👤 **用户名**: root
          - 🔑 **密码**: 无
          EOF

          if [ "${{ github.event.inputs.include_docker }}" == "yes" ]; then
            # 格式与上方保持完全一致
            echo "- 🧩 **Docker 状态**: 已集成" >> ${{ github.workspace }}/router-info.md
          else
            echo "- 🧩 **Docker 状态**: 未集成" >> ${{ github.workspace }}/router-info.md
          fi

      - name: Upload OpenWrt as release assets
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: openwrt-${{ inputs.luci_version }}-x86-64-RELEASE
          body_path: ${{ github.workspace }}/router-info.md
          files: |
            ${{ github.workspace }}/bin/targets/x86/64/*squashfs-combined-efi.img.gz
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
